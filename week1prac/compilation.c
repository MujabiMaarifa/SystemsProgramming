#include <stdio.h>

int main() {
    printf("Hellow world");
        return 0;
}

/*How compilation occurs in C
 *Preprocessing - handles the directives, macros and header files
 *Compilation - changes the source code to assembly code
 *Assembly - changes the assembly code into objects
 *linking - links the object files and libraries into executable
 * */
